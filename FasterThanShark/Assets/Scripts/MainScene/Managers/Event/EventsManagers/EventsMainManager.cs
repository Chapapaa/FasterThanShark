using UnityEngine;
using System.Collections;

public class EventsMainManager : MonoBehaviour {

    public EventsDatabase evntDtb;
    public EnemyShipDatabase enemyShipDtb;





    /*  
        Escape();
        Next();
        GO ? y/n
        aff map
        DestChosen(eventType)
        GO event = EvntDtb.getEvent
        Inst event
        Si event stantard
            affiche l'event et place la camera
        Si event de combat
            affiche l'event et l'enemy et la camera
        
        



    */

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
